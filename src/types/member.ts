// 로그인/회원정보 profile 타입
export interface MemberProfile {
  name: string;
  email: string;
  phoneNumber: string;
}
export {};

/*
export interface SignUpResponse {
  token: string | null;
}

export interface LoginResponse {
  token: string | null;
  name: string | null;
}
*/
